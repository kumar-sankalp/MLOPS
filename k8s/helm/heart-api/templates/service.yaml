apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-heart-api-service
  labels:
    app: heart-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: heart-api
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
