# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
venv/
.venv/
.env

# MLflow local backend (regenerated by `python -m src.train`)
mlflow.db
mlartifacts/
mlruns/

# OS
.DS_Store
Thumbs.db

# Keep the trained model + metadata tracked so the Docker build works
# without retraining. (Comment the next two lines out if you prefer to
# retrain in CI and NOT commit binaries.)
# models/model.joblib
# models/model_metadata.json

# GitHub Actions Runner
actions-runner/

# Reports
# (Final report is now tracked)
